import React from "react";
import { connect } from "react-redux";
import { Link } from "react-router-dom";

export const Nav = () => {
  return <h3 className="zilch">Nav</h3>;
};

export default Nav;
